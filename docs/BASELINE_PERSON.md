# 电脑端人物检测基线

## 输入与配置

| 项目 | 值 |
|---|---|
| 测试视频 | `37abaa4512176295e7462a589a64674a.mp4` |
| 视频 SHA-256 | `dcf3f683111349479927bd962223bfbc9fca325d1bb25da8efb74f489b641209` |
| 分辨率 | 1280×960 |
| 帧率/帧数 | 30.105 FPS / 57 帧 |
| 时长 | 1.893 秒 |
| 模型 | YOLO11n，COCO `person` 类别 ID 0 |
| 输入尺寸 | 640 |
| 置信度阈值 | 0.25 |
| 设备 | Intel Core i5-12400F，CPU 推理 |

模型文件 `yolo11n.pt` 的本次 SHA-256 为 `0ebbc80d4a7680d14987a577cd21342b65ecfd94632bd9a8da63ae6417644ee1`。

## 软件环境

- Python 3.12.3
- Ultralytics 8.4.113
- PyTorch 2.13.0+cpu
- OpenCV 5.0.0

## 结果

| 指标 | 结果 |
|---|---:|
| 成功解码/输出 | 57 / 57 帧 |
| 总检测框 | 1032 |
| 每帧人物框 | 最少 16、最多 20、平均 18.11 |
| 模型冷启动 | 779.00 ms |
| 稳态推理 P50 | 32.84 ms |
| 稳态推理 P95 | 37.08 ms |
| 稳态推理 P99 | 37.64 ms |
| 含解码、绘制、写盘的吞吐率 | 23.85 FPS |

代表帧人工检查显示，模型能在站队密集、人物相互靠近的场景中分离多数人物。画面左右边缘的截断人物和重叠区域仍是后续重点测试对象。

这些数字是检测输出统计，不是精度指标。当前视频没有逐人标注，因此不能据此计算 precision、recall 或漏检率；建立人工标注测试集后再进行正式精度验收。

## 复现

```bash
make detect-person VIDEO=/absolute/path/to/37abaa4512176295e7462a589a64674a.mp4
```

结果保存在 `outputs/person_baseline/`，包括带框视频、逐帧 JSONL、代表帧和机器可读汇总。输出和模型权重默认不提交 Git。

## RK3588 迁移依据

电脑端先使用 YOLO11n 固化结果格式和回归视频。Rockchip 官方 [RKNN Model Zoo](https://github.com/airockchip/rknn_model_zoo) 已包含 RK3588 的 YOLO11 示例和 ONNX→RKNN 转换流程；上板时替换推理适配器，并用同一视频对比电脑端与 RKNN 的框、置信度、吞吐和量化精度损失。

Ultralytics 软件和模型的使用、分发应在正式产品化前单独完成许可证审查。
